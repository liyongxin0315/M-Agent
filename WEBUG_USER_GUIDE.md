# AgentM WebUI 使用指南

## 📖 目录

1. [快速入门](#快速入门)
2. [功能模块说明](#功能模块说明)
3. [监控仪表板](#监控仪表板)
4. [工作流编辑器](#工作流编辑器)
5. [执行历史](#执行历史)
6. [错误日志](#错误日志)
7. [熔断器配置](#熔断器配置)
8. [API 接口](#api 接口)
9. [常见问题](#常见问题)

---

## 快速入门

### 启动 WebUI

```bash
# 进入项目目录
cd /home/liyongxin/.openclaw/workspace/agentm

# 安装依赖（首次运行）
pip install flask flask-socketio psutil

# 启动服务
python webui/webui.py

# 或使用环境变量自定义
export PORT=8080
export DEBUG=true
python webui/webui.py
```

### 访问界面

打开浏览器访问：**http://localhost:5000**

### 界面导航

```
┌─────────────────────────────────────────────────────────┐
│  🤖 AgentM 监控仪表板                     ● 已连接       │
├─────────────────────────────────────────────────────────┤
│  [📊 总览] [✏️ 工作流编辑器] [📋 执行历史]              │
│  [📝 错误日志] [⚡ 熔断器]                               │
├─────────────────────────────────────────────────────────┤
│                                                         │
│                    主要内容区域                          │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 功能模块说明

### 模块总览

| 模块 | 功能 | 访问路径 |
|------|------|----------|
| 📊 总览 | 系统监控、性能指标 | `/` |
| ✏️ 工作流编辑器 | 可视化创建工作流 | `#workflow-editor` |
| 📋 执行历史 | 查看执行记录 | `#execution-history` |
| 📝 错误日志 | 查看系统日志 | `#error-log` |
| ⚡ 熔断器 | 熔断器状态和配置 | `#circuit-breaker` |

---

## 监控仪表板

### CPU 使用率监控

**位置：** 总览页左上角

**功能：**
- 实时显示当前 CPU 使用率百分比
- 进度条直观展示负载程度
- 折线图显示最近 30 秒趋势

**颜色说明：**
- 🟦 蓝色：正常（<50%）
- 🟨 黄色：警告（50-70%）
- 🟥 红色：高负载（>70%）

### 内存使用率监控

**位置：** 总览页右上角

**功能：**
- 显示当前内存使用百分比
- 显示已用/总内存（GB）
- 折线图显示最近 30 秒趋势

### 工作流统计

**位置：** 总览页中部

**指标说明：**
- **总执行次数：** 系统启动以来的总执行数
- **成功：** 成功完成的工作流数量
- **失败：** 执行失败的工作流数量
- **成功率：** 成功数/总数 × 100%

### 系统信息

**位置：** 总览页右下角

**信息项：**
- **运行时间：** 系统连续运行时长
- **活跃工作流：** 当前正在执行的工作流数
- **队列任务：** 等待执行的任务数
- **平均响应时间：** 请求平均处理时间（毫秒）

### 性能趋势图

**位置：** 总览页底部

**功能：**
- 同时显示 CPU 和内存使用趋势
- 50 秒滚动窗口
- 便于对比分析资源使用情况

---

## 工作流编辑器

### 节点类型

| 节点 | 图标 | 说明 |
|------|------|------|
| 🔵 开始节点 | 蓝色 | 工作流起点 |
| 🔶 API 调用 | 橙色 | 调用外部 API |
| 🟢 数据库操作 | 绿色 | 数据库读写 |
| 🟡 数据转换 | 黄色 | 数据处理转换 |
| 🔷 条件判断 | 深蓝 | 条件分支 |
| 🟣 循环 | 紫色 | 循环执行 |
| 🟠 AI 处理 | 橙红 | AI 模型调用 |
| 🔴 结束节点 | 红色 | 工作流终点 |

### 操作步骤

#### 创建节点

1. 从顶部节点面板拖拽节点到画布
2. 松开鼠标，节点创建成功
3. 可拖拽节点调整位置

#### 编辑节点

1. 点击节点上的「编辑」按钮
2. 修改节点名称和配置
3. 配置支持 JSON 格式
4. 点击「保存」完成编辑

**节点配置示例：**
```json
{
  "timeout": 30,
  "retry_count": 3,
  "params": {
    "key": "value"
  }
}
```

#### 连接节点

*（当前版本支持节点创建，连接功能开发中）*

#### 删除节点

1. 点击节点上的「删除」按钮
2. 确认删除操作

#### 保存工作流

1. 点击「💾 保存工作流」按钮
2. 输入工作流名称
3. 确认保存

#### 运行工作流

1. 点击「▶️ 运行工作流」按钮
2. 确认运行
3. 查看执行结果

#### 清空画布

1. 点击「🗑️ 清空画布」按钮
2. 确认清空（不可恢复）

---

## 执行历史

### 查看执行记录

**位置：** 执行历史标签页

**列表字段：**
- **ID：** 执行记录唯一标识
- **工作流名称：** 执行的工作流名称
- **状态：** 执行状态（完成/失败/运行中/等待）
- **开始时间：** 执行开始时间
- **耗时：** 执行总耗时（秒）
- **操作：** 详情/重跑按钮

### 状态标识

| 状态 | 颜色 | 说明 |
|------|------|------|
| completed | 🟢 绿色 | 执行成功 |
| failed | 🔴 红色 | 执行失败 |
| running | 🔵 蓝色 | 正在执行 |
| pending | 🟠 橙色 | 等待执行 |

### 查看详情

点击「详情」按钮查看：
- 工作流完整配置
- 每个步骤的执行结果
- 错误信息（如有）
- 详细时间线

### 重新执行

点击「重跑」按钮：
1. 确认重新执行
2. 系统创建新的执行记录
3. 跳转到执行详情页面

---

## 错误日志

### 日志级别

| 级别 | 颜色 | 说明 |
|------|------|------|
| ERROR | 🔴 红色 | 错误，需要关注 |
| WARNING | 🟨 黄色 | 警告，可能有问题 |
| INFO | 🟦 蓝色 | 信息，正常日志 |

### 日志过滤

使用级别下拉框筛选：
- **全部级别：** 显示所有日志
- **错误：** 仅显示 ERROR 级别
- **警告：** 仅显示 WARNING 级别
- **信息：** 仅显示 INFO 级别

### 日志格式

```
2026-04-01 12:34:56  ERROR   [workflow_engine] 步骤执行失败：连接超时
2026-04-01 12:34:55  WARNING [circuit_breaker] 失败次数达到阈值
2026-04-01 12:34:54  INFO    [api_service] 请求处理完成
```

### 操作按钮

- **🔄 刷新日志：** 重新加载最新日志
- **🗑️ 清空日志：** 删除所有日志记录（需确认）

---

## 熔断器配置

### 状态监控

**位置：** 熔断器标签页顶部

**监控卡片：**
- **API 调用：** 外部 API 服务熔断状态
- **数据库连接：** 数据库服务熔断状态
- **AI 服务：** AI 模型服务熔断状态
- **文件系统：** 文件操作熔断状态

### 熔断器状态

| 状态 | 颜色 | 说明 |
|------|------|------|
| CLOSED | 🟢 绿色 | 正常，允许请求 |
| OPEN | 🔴 红色 | 熔断，拒绝请求 |
| HALF_OPEN | 🟨 黄色 | 测试，有限请求 |

### 配置参数

| 参数 | 说明 | 默认值 | 范围 |
|------|------|--------|------|
| 失败阈值 | 连续失败多少次触发熔断 | 5 | 1-100 |
| 恢复时间 | 熔断后多少秒尝试恢复 | 30 | 1-3600 |
| 半开最大请求 | 半开状态允许的最大请求数 | 3 | 1-10 |
| 成功率阈值 | 成功率低于多少触发告警 | 50% | 1-100 |

### 保存配置

1. 修改配置参数
2. 点击「💾 保存配置」
3. 配置立即生效

---

## API 接口

### 基础信息

- **基础 URL：** `http://localhost:5000/api`
- **数据格式：** JSON
- **认证：** 当前无需认证

### 接口列表

#### 获取系统统计

```http
GET /api/stats
```

**响应：**
```json
{
  "cpu": {"percent": 45.2},
  "memory": {"percent": 62.5, "used": 8589934592, "total": 17179869184},
  "workflow": {"total": 100, "completed": 95, "failed": 5, "active": 2}
}
```

#### 获取执行历史

```http
GET /api/executions
```

**响应：**
```json
[
  {
    "id": "exec_1",
    "workflow_name": "data_sync",
    "status": "completed",
    "start_time": "2026-04-01T12:00:00",
    "total_duration": 5.23
  }
]
```

#### 获取执行详情

```http
GET /api/execution/<execution_id>
```

#### 运行工作流

```http
POST /api/run
Content-Type: application/json

{
  "workflow_type": "data_sync",
  "config": {
    "source": {"type": "mysql"},
    "target": {"type": "postgres"}
  }
}
```

**响应：**
```json
{
  "success": true,
  "execution_id": "exec_2"
}
```

#### 保存工作流

```http
POST /api/workflow
Content-Type: application/json

{
  "name": "my_workflow",
  "type": "custom",
  "config": {},
  "nodes": []
}
```

#### 删除工作流

```http
DELETE /api/workflow/<workflow_id>
```

#### 获取熔断器状态

```http
GET /api/circuit-breaker/status
```

**响应：**
```json
{
  "circuits": {
    "api": {"state": "CLOSED", "failures": 0, "threshold": 5},
    "db": {"state": "CLOSED", "failures": 1, "threshold": 3}
  }
}
```

#### 更新熔断器配置

```http
POST /api/circuit-breaker/config
Content-Type: application/json

{
  "failureThreshold": 5,
  "recoveryTimeout": 30,
  "halfOpenMax": 3,
  "successThreshold": 50
}
```

#### 获取日志

```http
GET /api/logs?limit=100
```

**响应：**
```json
{
  "logs": [
    {
      "timestamp": "2026-04-01T12:00:00",
      "level": "ERROR",
      "message": "连接超时"
    }
  ]
}
```

#### 清空日志

```http
DELETE /api/logs
```

---

## WebSocket 实时推送

### 连接地址

```
ws://localhost:5000/ws
```

### 订阅频道

```json
{
  "action": "subscribe",
  "channels": ["cpu", "memory", "workflow", "log", "circuit"]
}
```

### 推送消息格式

**CPU 更新：**
```json
{
  "type": "cpu",
  "value": {"percent": 45.2}
}
```

**内存更新：**
```json
{
  "type": "memory",
  "value": {"percent": 62.5, "used": 8589934592, "total": 17179869184}
}
```

**工作流更新：**
```json
{
  "type": "workflow",
  "value": {"total": 100, "completed": 95, "failed": 5}
}
```

**日志更新：**
```json
{
  "type": "log",
  "value": {
    "timestamp": "2026-04-01T12:00:00",
    "level": "ERROR",
    "message": "连接超时"
  }
}
```

**熔断器更新：**
```json
{
  "type": "circuit",
  "value": {
    "circuits": {
      "api": {"state": "OPEN", "failures": 5}
    }
  }
}
```

---

## 常见问题

### Q1: WebUI 无法启动

**现象：** 运行 `python webui.py` 后报错

**可能原因：**
1. 端口被占用
2. 依赖未安装

**解决方案：**
```bash
# 检查端口占用
lsof -i :5000

# 更换端口
export PORT=8080
python webui.py

# 安装依赖
pip install flask flask-socketio psutil
```

### Q2: WebSocket 连接失败

**现象：** 右上角显示「已断开」

**可能原因：**
1. Flask-SocketIO 未安装
2. 浏览器不支持 WebSocket

**解决方案：**
```bash
pip install flask-socketio
```

### Q3: 图表不显示

**现象：** CPU/内存图表区域空白

**可能原因：**
1. Chart.js CDN 加载失败
2. 浏览器网络问题

**解决方案：**
1. 检查网络连接
2. 刷新页面
3. 检查浏览器控制台错误

### Q4: 工作流编辑器无法拖拽

**现象：** 节点无法拖拽到画布

**可能原因：**
1. 浏览器兼容性问题
2. JavaScript 错误

**解决方案：**
1. 使用 Chrome/Firefox 浏览器
2. 检查浏览器控制台错误
3. 清除浏览器缓存

### Q5: 熔断器配置不生效

**现象：** 保存配置后无变化

**可能原因：**
1. 后端未实现配置接口
2. 配置未持久化

**解决方案：**
1. 检查后端日志
2. 确认配置接口返回成功
3. 重启服务后检查

### Q6: 内存使用率显示异常

**现象：** 内存使用率超过 100% 或显示负数

**可能原因：**
1. psutil 库版本问题
2. 系统兼容性问题

**解决方案：**
```bash
# 更新 psutil
pip install --upgrade psutil

# 重启服务
python webui/webui.py
```

---

## 技术支持

### 日志位置

```
/home/liyongxin/.openclaw/workspace/agentm/logs/
```

### 配置文件

```
/home/liyongxin/.openclaw/workspace/agentm/config.yaml
```

### 相关文档

- [优化报告](./OPTIMIZATION_REPORT.md)
- [README](./README.md)

---

*文档版本：v1.0*  
*最后更新：2026-04-01*
