# API Integration Skill - API 集成技能

支持 REST 和 GraphQL API 调用

## 功能
- RESTful API 请求（GET/POST/PUT/DELETE/PATCH）
- GraphQL 查询和变更
- 认证管理（Bearer Token, API Key, Basic Auth）
- 请求重试和超时控制
- 响应缓存

## 依赖
```bash
pip install httpx gql[transport-httpx]
```
